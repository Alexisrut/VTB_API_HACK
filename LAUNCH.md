# 🚀 Команды запуска сервиса

## Быстрый старт

### 1. Запуск основного приложения

**Самый простой способ:**
```bash
./start.sh
```

Этот скрипт:
- ✅ Проверит наличие `.env` файла
- ✅ Запустит PostgreSQL и Redis через Docker
- ✅ Дождется готовности сервисов
- ✅ Запустит FastAPI приложение

### 2. Ручной запуск (пошагово)

#### Шаг 1: Запуск инфраструктуры (PostgreSQL + Redis)
```bash
docker-compose up -d postgres redis
```

#### Шаг 2: Проверка готовности сервисов
```bash
# Проверка PostgreSQL
docker-compose exec postgres pg_isready -U fastapi_user

# Проверка Redis
docker-compose exec redis redis-cli ping
```

#### Шаг 3: Запуск FastAPI приложения
```bash
# Активация виртуального окружения (если используется)
source venv/bin/activate

# Запуск приложения
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

### 3. Запуск через Docker Compose (все сервисы)

```bash
docker-compose up
```

Это запустит:
- PostgreSQL (порт 5432)
- Redis (порт 6379)
- FastAPI приложение (порт 8000)

## Запуск фоновых задач (Celery)

### Автоматический запуск
```bash
./start_celery.sh
```

### Ручной запуск

**Terminal 1 - Celery Worker:**
```bash
celery -A app.tasks.sync_tasks.celery_app worker --loglevel=info
```

**Terminal 2 - Celery Beat (периодические задачи):**
```bash
celery -A app.tasks.sync_tasks.celery_app beat --loglevel=info
```

### Остановка Celery
```bash
./stop_celery.sh
```

## Проверка работы

После запуска проверьте:

1. **Health check:**
```bash
curl http://localhost:8000/health
```

2. **API документация:**
Откройте в браузере: http://localhost:8000/docs

3. **Проверка подключения к БД:**
```bash
docker-compose exec postgres psql -U fastapi_user -d fastapi_db -c "SELECT 1;"
```

## Остановка сервисов

### Остановка контейнеров (без удаления)
```bash
./stop.sh
```
Останавливает контейнеры, но сохраняет их и данные. Можно быстро перезапустить через `./start.sh`.

### Полная остановка и удаление контейнеров
```bash
./stop_all.sh
```
Останавливает и удаляет контейнеры. Данные в volumes сохраняются.

### Остановка только приложения (Ctrl+C)
Если запускали через `uvicorn`, нажмите `Ctrl+C` в терминале

### Остановка Celery
```bash
./stop_celery.sh
```

### Ручная остановка через Docker Compose
```bash
# Остановить контейнеры
docker compose stop

# Остановить и удалить контейнеры
docker compose down

# Остановить, удалить контейнеры и volumes (⚠️ удалит данные БД!)
docker compose down -v
```

## Переменные окружения

Убедитесь, что файл `.env` содержит:

```env
DATABASE_URL=postgresql+asyncpg://fastapi_user:fastapi_password@localhost:5432/fastapi_db
REDIS_URL=redis://localhost:6379/0
SECRET_KEY=your-secret-key-here
```

## Troubleshooting

### Порт уже занят
Если порт 8000 занят, используйте другой:
```bash
uvicorn main:app --host 0.0.0.0 --port 8001 --reload
```

### PostgreSQL не запускается
```bash
# Проверьте логи
docker-compose logs postgres

# Пересоздайте контейнер
docker-compose down
docker-compose up -d postgres
```

### Redis не запускается
```bash
# Проверьте логи
docker-compose logs redis

# Пересоздайте контейнер
docker-compose down
docker-compose up -d redis
```

### Ошибки импорта модулей
Убедитесь, что все зависимости установлены:
```bash
pip install -r requirements.txt
```

### Проблемы с миграциями БД
База данных создается автоматически при первом запуске через SQLAlchemy `create_all()`.
Если нужно пересоздать таблицы, остановите приложение и удалите том PostgreSQL:
```bash
docker-compose down -v
docker-compose up -d postgres
# Затем запустите приложение снова
```

